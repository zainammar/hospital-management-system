[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[start]
cmd = "python manage.py migrate --noinput && python setup_demo.py; gunicorn hospital_management.wsgi:application --bind 0.0.0.0:$PORT"
